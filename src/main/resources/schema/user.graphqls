type Query {
    users: [User]
    user(id: ID) : User
}

type User {
    id: ID!
    name: String
}

input UserUpdated {
    id: ID!
    name: String
}

type Mutation {
    createUser(name: String! ) : User!
    deleteUser(id: Int!) : Boolean!
    updateUser(user: UserUpdated! ) : User!
}