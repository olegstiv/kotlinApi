type Query {
    users: [User]
    user(id: ID) : User
}

type User {
    id: ID!
    name: String
}

input UserInput {
    name: String
}

type Mutation {
    createUser(name: String! ) : User!
    deleteUser(id: Int!) : Boolean!
    updateUser(id: Int!, user: UserInput ) : User!
}